# ─── AI Gateway 开发配置（仅源码运行时生效）────────────────────────
# 本文件由 src/license.py 的 _load_dotenv() 在启动时读取。
# 打包版（Nuitka/PyInstaller）不会读到本项目根的这个文件，且编译标志会硬卡
# 开发豁免 —— 生产用户自行设置也不生效。本文件是开发便利，不是后门。

# 开发豁免授权：GW_DEV=1 时跳过激活/验证（重启无感，调试授权流程时改回 0）。
GW_DEV=1
